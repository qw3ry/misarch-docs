---
sidebar_position: 2
---

import CustomImage from "@site/src/components/CustomImage";

# Component Diagram

Overview of the main components and their interactions in the Misarch architecture.

In addition, each service has an associated database. These are omitted in the overview picture. The communication between the components works via dapr and event buses, detailed below the component diagram. A **dottet line** denotes synchronous communication, a **solid line** asynchronous communication

<CustomImage path="/renderedDiagrams/component" width="2702.5" height="1678.5" />

## Communication Between Service Components

The service components communicate via _dapr_. The **synchronous communication** (dottet line, dapr service invocation) and **asynchronous communication** (solid line, dapr pub/sub via event bus) works as follows:

<CustomImage path="/renderedDiagrams/componentInteraction" width="1330" height="730" />